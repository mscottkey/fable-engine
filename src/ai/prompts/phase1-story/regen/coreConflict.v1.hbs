CURRENT STORY OVERVIEW
{{json currentData}}

TASK
Regenerate only the coreConflict field based on the campaign seed and current story context.

FEEDBACK
{{feedback}}

RULES
- Should be a concise statement (2-3 sentences) that captures the central dramatic tension.
- Must connect to the story hooks and setting.
- Return only the coreConflict as a JSON string value (not an object).
- Output STRICT JSON only.
