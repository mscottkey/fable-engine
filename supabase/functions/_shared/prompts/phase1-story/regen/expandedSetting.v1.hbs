CURRENT STORY OVERVIEW
{{json currentData}}

TASK
Regenerate only the expandedSetting field based on the campaign seed and current story context.

FEEDBACK
{{feedback}}

RULES
- Keep it evocative yet concise (2-4 tight paragraphs).
- Maintain consistency with the genre, tone levers, and locations.
- Return only the expandedSetting as a JSON string value (not an object).
- Output STRICT JSON only.
