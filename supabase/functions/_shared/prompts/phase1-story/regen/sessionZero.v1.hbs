CURRENT STORY OVERVIEW
{{json currentData}}

TASK
Regenerate only the sessionZero object based on the campaign seed and current story context.

FEEDBACK
{{feedback}}

RULES
- Include openQuestions (things to establish at the table), contentAdvisories (potential sensitive topics), and calibrationLevers (adjustable dials for the campaign).
- Each should be an array of strings.
- Keep items concise and actionable.
- Return only the sessionZero object as JSON.
- Output STRICT JSON only.
