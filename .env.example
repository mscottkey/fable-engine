# Example env for Vite frontend (do NOT commit secrets to git)
# Copy to .env.local and fill with your project's values

# === Client-Side Environment Variables (VITE_*) ===
# These are safe to expose to the browser

# Supabase connection (anon key is safe to expose)
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""

# === Server-Side Environment Variables ===
# These MUST be kept secret and only used in edge functions
# Set these in your deployment platform (Vercel, Supabase, etc.)

# Lovable AI Gateway API key (used by edge functions)
LOVABLE_API_KEY=""

# Salt used to hash prompt/completion text for logging
AI_LOG_SALT=""

# === Deployment Instructions ===
#
# For Vercel:
# 1. Add VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY as environment variables
# 2. Do NOT add LOVABLE_API_KEY or AI_LOG_SALT to Vercel (these are Supabase edge function secrets)
#
# For Supabase Edge Functions:
# Run: supabase secrets set LOVABLE_API_KEY=your-key
# Run: supabase secrets set AI_LOG_SALT=your-salt
#
# IMPORTANT: Never expose LOVABLE_API_KEY or AI_LOG_SALT to the client
# All AI operations now run through secure edge functions
