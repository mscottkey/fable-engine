# Example env for Vite frontend (do NOT commit secrets to git)
# Copy to .env.local and fill with your project's values

# Vite client keys used by the frontend
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""

# === AI Gateway Configuration (Hybrid Mode) ===
#
# DEVELOPMENT MODE (Optional):
# Set VITE_LOVABLE_API_KEY to enable direct API calls from client (faster iteration)
# WARNING: This exposes the API key in the browser bundle - use only for local dev!
# VITE_LOVABLE_API_KEY=""
#
# PRODUCTION MODE (Automatic):
# When VITE_LOVABLE_API_KEY is not set, all AI calls route through ai-gateway edge function
# The edge function uses server-side LOVABLE_API_KEY (configured in Lovable Cloud secrets)
# This is secure and production-ready.

# Salt used to hash prompt/completion text for logging (keep secret)
AI_LOG_SALT="replace-with-random-hex-or-uuid"
